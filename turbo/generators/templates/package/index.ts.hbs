/** @platform/{{ kebabCase name }} — {{ description }} */

/** Sample export — replace with the package's real public API. */
export function {{ camelCase name }}Name(): string {
  return "{{ kebabCase name }}";
}
