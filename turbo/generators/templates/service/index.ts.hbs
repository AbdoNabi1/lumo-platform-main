/**
 * @platform/{{ kebabCase name }} — {{ description }}
 *
 * Bounded-context service (clean-architecture slice: domain / application / infrastructure /
 * interfaces). Add aggregates with `pnpm gen aggregate` and use-cases with `pnpm gen use-case`,
 * then export the public surface (composition root, controllers) here.
 */
export {};
