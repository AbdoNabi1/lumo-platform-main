{
  "name": "@platform/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end browser tests (Playwright) for the storefront and admin-web apps — Phase 6 T6.1, the safety net's first layer.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.7.5",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
