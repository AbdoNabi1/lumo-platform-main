{
  "name": "@platform/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Outbound service contracts (interfaces only): IdGenerator, Clock. No implementations, no DI, no framework code, no runtime dependencies.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet\""
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
