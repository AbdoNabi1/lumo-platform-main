{
  "name": "@platform/usage",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical generic usage-metering contract (ADR-0018 §C) — platform.usage.recorded event + UsageRecorderPort. Producers know no consumers (Licensing/Billing/Analytics/AI consume).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@platform/contracts": "workspace:*",
    "@platform/domain": "workspace:*",
    "@platform/messaging": "workspace:*",
    "@platform/registry": "workspace:*",
    "@platform/types": "workspace:*",
    "@platform/utils": "workspace:*"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
