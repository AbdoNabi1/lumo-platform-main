{
  "name": "@platform/redis",
  "version": "0.0.0",
  "private": true,
  "description": "Redis infrastructure: connection manager, retry strategy, cache abstraction, serialization, health. No business caching.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@platform/config": "workspace:*",
    "@platform/contracts": "workspace:*",
    "@platform/health": "workspace:*",
    "@platform/utils": "workspace:*",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@types/node": "^22.7.5",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
