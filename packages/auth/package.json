{
  "name": "@platform/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Identity infrastructure adapters: JWKS JWT verifier, Ory Kratos session/identity adapters, Ory Keto AccessControl with decision caching — all behind the existing contracts ports (Sprint 2.7).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@platform/contracts": "workspace:*",
    "@platform/utils": "workspace:*",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@types/node": "^22.7.5",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
