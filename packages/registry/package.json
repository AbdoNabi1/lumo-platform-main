{
  "name": "@platform/registry",
  "version": "0.0.0",
  "private": true,
  "description": "Registry Engine (Platform Kernel, ADR-0055) — the reusable versioned/tenant-agnostic registration primitive every registry composes: registration, lookup, versioning, validation, discovery, metadata, tags, lifecycle. Pure infrastructure, no business logic.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@platform/types": "workspace:*",
    "@platform/utils": "workspace:*"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
