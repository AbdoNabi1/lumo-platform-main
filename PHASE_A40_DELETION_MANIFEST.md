# Phase A.40 — Deletion Manifest

Continuing the evidence-based cleanup discipline from Phases A.35/A.36. Candidates were sought
across the repository; the bar for DELETE is positive proof of zero references across runtime,
build, CI, tests, deployment, and workspace configs. Insufficient evidence → KEEP.

## Candidates Considered

| #   | Path                                                                                                                        | Reason Considered                                                                                                                                                                                                                  | Evidence / References Searched                                                                                                                                                                                                             | Build/Runtime/CI/Deploy Impact                                                                                                        | Decision                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `apps/admin-web/.next/cache/webpack/*/index.pack.old` (and equivalent under `apps/storefront/.next/cache/...`)              | Webpack incremental-cache `.old` backup files found during a filesystem sweep for `*.bak`/`*.orig`/`*.old`                                                                                                                         | `git check-ignore apps/admin-web/.next` → ignored (confirmed via `git status --short`, neither `.next` tree appears as tracked/untracked in git). These are Next.js's own build-cache artifacts, regenerated every build, never committed. | None — not part of the repository; deleting or leaving them has zero effect on build/CI/deploy since they are outside version control | KEEP — not repository content, out of scope for a source deletion manifest (build cache, gitignored)                                                                       |
| 2   | `.pnpm-store/v11/index.db`                                                                                                  | Already shown as `D` (staged/working delete) in `git status --short` from a **prior** phase (A.36 per `PHASE_A36_RUNTIME_BUILD_CRITICALITY_REPORT.md` line 126: "accidentally-tracked local package-store cache index... removed") | Confirmed still present as a pending deletion in today's `git status --short` baseline capture (Phase 1)                                                                                                                                   | Already validated in A.36 (all gates green before/after)                                                                              | KEEP AS-IS — this is prior-phase uncommitted work; Phase A.40 must not touch, commit, or alter it per the explicit constraint to leave existing uncommitted work untouched |
| 3   | Root-level `PHASE_A*.md` historical report files (A30–A39)                                                                  | Considered whether any are now superseded/redundant now that A.40 exists                                                                                                                                                           | These are historical audit-trail documents, actively cross-referenced by this phase and by memory index entries (e.g. `lumo-phase-a31-commit-push-milestone.md`). Constraint explicitly says "don't rewrite historical reports" (Phase 17) | None — documentation only, no build/runtime/CI/deploy relationship                                                                    | KEEP — historical audit trail, explicitly out of scope for deletion                                                                                                        |
| 4   | `apps/admin-web/src/app/api/*/route.ts` (single route found: healthz)                                                       | Checked for orphaned/dead API routes not wired into any nav or client call                                                                                                                                                         | Only 1 `route.ts` exists under `apps/admin-web/src/app/api` (health check), actively required by container/k8s liveness probes per `infrastructure/k8s/*.yaml`                                                                             | Removing would break k8s health probe wiring                                                                                          | KEEP — actively referenced                                                                                                                                                 |
| 5   | Repository-wide search for `*.bak`, `*.orig`, `*.old`, `*.tmp`, `*-copy.*`, `*_old.*` naming patterns outside `.next` cache | General dead-file sweep per Phase 14 mandate                                                                                                                                                                                       | `find . -iname "*.bak" -o -iname "*.orig" -o -iname "*.old"` (excluding `node_modules`, `.git`) returned only the 7 gitignored `.next/cache/webpack/**/*.pack.old` entries listed in row 1                                                 | N/A                                                                                                                                   | KEEP — no additional candidates found                                                                                                                                      |

## Outcome

**Zero deletions made in Phase A.40.** This is a valid and, per the prior phases' cleanup history
(A.35/A.36 already removed the tracked cache-index artifact; no further tracked dead files exist),
an expected outcome — the repository was already swept for dead/duplicate/orphaned files across
two prior cleanup phases (A.35, A.36) and no new tracked candidates met the positive-evidence bar
this phase.

| Decision | Count |
| -------- | ----- |
| DELETE   | 0     |
| KEEP     | 5     |
| DEFER    | 0     |
