# Medium Findings Register — Independent Verification Matrix

**Audited at:** HEAD `4346c81` (branch `main`), 2026-08-06.
**Baseline register:** `MEDIUM_VERIFICATION_REPORT.md` (M2-1 through M2-7, re-verified against `HEAD 8781f07`).
**Method:** for each finding, read the closing report's claims, then independently re-derive from
current source — opened the claimed files, ran `git show --stat`/`git log` against the claimed
commits, and re-ran representative greps/counts rather than trusting the report's own numbers.

| Finding ID | Root Cause                                                                                                                                                                                                                                         | Closing Commit                                                                                                                                                  | Current Status                      | Independently Verified | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **M2-1**   | Purchase saga (`@platform/temporal`) has zero importers repo-wide; no `.signal()` call site exists outside the unrelated `TrackingRegistryStore.signal()`. Purchases complete only via the synchronous HTTP fallback.                              | N/A — never closed, intentionally                                                                                                                               | **DEFERRED (legitimate)**           | Yes                    | `grep "@platform/temporal"` across all `.ts` → 0 matches. `grep "\.signal\("` → only `apps/runtime/src/composition.ts:330` and `apps/runtime/src/tracking/tracking-registry-handle.ts:218`, both `TrackingRegistryHandle`/`TrackingRegistryStore.signal()` — an unrelated tracking hot-reload primitive, not a Temporal workflow signal. `MEDIUM_REMEDIATION_PLAN.md:55-62,126-142,216` explicitly documents the dependency on C2-2 (real PSP adapter, still Critical/open) and recommends deferral rather than a standalone sprint. `POST_CRITICAL_VERIFICATION_REPORT.md:556-558` independently confirms C2-2's PSP half "was never claimed to be closed and remains open" — the dependency is real, not a pretext.                                                                                                                                                                                                              |
| **M2-2**   | `InMemoryObjectStorage.exists()` always `true`; `StorageServiceObjectStorage` unconditionally threw; no `apps/` entrypoint ever imported `@platform/storage` or supplied `objectStorage`.                                                          | `9f36a69` "fix(media): wire real S3/MinIO object storage + fail-closed boot guard (M2-2)"                                                                       | **CLOSED**                          | Yes                    | `git show --stat 9f36a69` touches exactly the 12 claimed files (14 incl. report/lockfile). Read `services/media/src/infrastructure/object-storage-adapters.ts`: `StorageServiceObjectStorage` now delegates `exists()`→`storageService.objectExists()` and `getDownloadUrl()`→`storageService.getSignedDownloadUrl()` — no throw remains. `apps/runtime/src/composition.ts:96,176,210` show `objectStorage: ObjectStoragePort` is resolved and threaded into `RuntimeCore`. `apps/admin/src/composition.ts:173` adds `objectStorage?: ObjectStoragePort` to `AdminWiringDeps`. `apps/runtime/src/api.ts:81-96,137` defines and calls `assertProductionObjectStorageConfigured`, which fails closed (`instanceof InMemoryObjectStorage` check) outside `APP_ENV=local`. `InMemoryObjectStorage` itself is intentionally unchanged (still the local/dev fallback, by design).                                                        |
| **M2-3**   | `services/licensing/src/composition.ts`'s `buildController` hardcoded `InMemoryPaymentsAdapter`/`InMemoryFinanceLedgerAdapter` with no injection seam; invoices always "collected" with no real payment or ledger entry.                           | `b10f7fc` "fix(licensing): add payments/financeLedger injection seam + fail-closed boot guard (M2-3)"                                                           | **CLOSED**                          | Yes                    | `git show --stat b10f7fc` touches exactly the 7 claimed files. Read `services/licensing/src/composition.ts:84-138`: `LicensingWiringDeps` now has optional `payments?`/`financeLedger?`; `buildController` reads `deps.payments ?? new InMemoryPaymentsAdapter()` / `deps.financeLedger ?? new InMemoryFinanceLedgerAdapter()` — genuine seam, not cosmetic. `apps/runtime/src/api.ts:53-67,136` defines and calls `assertProductionLicensingBillingConfigured`, throwing outside `local`. Note: this guard is `appEnv`-only (not adapter-identity-based like M2-2's), which matches the report's own disclosure that no real PSP adapter exists yet to distinguish "configured" from "not" — consistent, not a discrepancy.                                                                                                                                                                                                       |
| **M2-4**   | Two `as never` casts at Prisma-store construction sites defeated type checking (`apps/runtime/src/composition.ts`, `wire-tracking-runtime.ts`); separately, 165 `as unknown as` double-casts and 96 non-null assertions repo-wide, some avoidable. | `3297234` "fix(types): eliminate unsafe double-casts and non-null assertions..." + `21422b0` "fix(types): remove two dead `as never` casts..." (M2-4 follow-up) | **CLOSED**                          | Yes                    | `git show --stat` confirms both commits and their claimed diffs (3297234: 5+ files incl. `M2_4_REPORT.md`; 21422b0: exactly `composition.ts` + `wire-tracking-runtime.ts`, 2 lines each). `grep "as never"` on both files → **0 matches**, confirming removal. Independently recounted `as unknown as` repo-wide (services+apps+packages, `.ts`, excluding node_modules): **55 occurrences across 26 files** (42+18 files in `services`, 9+6 in `apps`, 4+2 in `packages`) — exact match to the report's claimed post-fix count "165 → 55 occurrences (54 → 26 files)".                                                                                                                                                                                                                                                                                                                                                            |
| **M2-5**   | `wireAnalytics()`/`wirePlatformConsole()` are correctly dependency-free, in-process read-models that reset on restart; the only real gap was an undocumented operational expectation.                                                              | `162536f` "docs(operations): disclose Analytics/Platform Console non-durable state (M2-5)"                                                                      | **CLOSED (docs-only, as intended)** | Yes                    | `git show --stat 162536f` touches exactly `docs/operations/OPERATIONS_GUIDE.md` (+ its own report) — no code files, matching the report's explicit "0 files code changed" claim. Read `docs/operations/OPERATIONS_GUIDE.md:49-59`: a "Known non-durable state" section exists, names both composition roots, explains the reset-on-restart behavior, and instructs on-call to check deploy history before paging. Composition roots themselves (`services/analytics/src/composition.ts`, `services/platform-console/src/composition.ts`) were correctly left untouched — this finding never called for a code change.                                                                                                                                                                                                                                                                                                              |
| **M2-6**   | No storefront container-image CI job and no `Deployment`/`Service`/`Ingress` in `infrastructure/k8s/` — customer-facing tier had no path to production.                                                                                            | `4346c81` "fix(storefront): deploy the storefront to production (M2-6)" (current HEAD)                                                                          | **CLOSED**                          | Yes                    | `git show --stat 4346c81` touches exactly the 9 claimed files (2 new k8s manifests + build.yml + 5 edited manifests + report). `.github/workflows/build.yml` now has a `storefront-image` job (line 132) building `web.Dockerfile`. `infrastructure/k8s/28-deployment-storefront.yaml` (read in full) is a genuine, non-stub Deployment: non-root uid 1001, dropped capabilities, read-only rootfs, topology spread, real startup/liveness/readiness probes against `/`, `envFrom` the new `27-storefront-config.yaml` ConfigMap. `infrastructure/k8s/kustomization.yaml` registers both new files (lines 15-16, tagged `# M2-6`). Report's disclosed residual gaps (no CSP on the Ingress yet; `deploy.yml`/`release.yml` don't pin/sign the storefront image, mirroring the pre-existing collector gap) are honestly flagged, not concealed, and don't undermine the finding's actual scope (deployability, not full CD parity). |
| **M2-7**   | `buildPaymentCapturedRuntime` (Kafka consumer path) constructed `MarkOrderPaid` without the optional `paymentVerification` dependency the admin HTTP path already supplied — consumer-path payment completion silently skipped verification.       | `50d9df4` "fix(orders): wire payment verification into the consumer path (M2-7)"                                                                                | **CLOSED**                          | Yes                    | `git show --stat 50d9df4` touches exactly the 3 claimed files. Read `apps/runtime/src/composition.ts:245-273`: `buildPaymentCapturedRuntime` now constructs `MarkOrderPaid` with `paymentVerification: new PrismaPaymentVerificationAdapter(core.prisma, core.config.TENANT_DEFAULT_ID)` — the identical adapter class the admin path (`api.ts`) already uses, with an explanatory `M2-7` comment at the call site. `services/orders/src/application/mark-order-paid.use-case.ts`'s `paymentVerification?` field remains optional (no breaking contract change), matching the report's claim that this was a wiring-only fix, not a contract change.                                                                                                                                                                                                                                                                               |

## Discrepancies

**None found.** Every closing report's file-level and commit-level claims matched actual repo state
exactly — commits exist, touch exactly the claimed files, and the claimed code changes are present
and functionally real (not stubs, not partial, not merely renamed). The one independently
recomputed metric (M2-4's `as unknown as` count) matched the report's own number exactly (55
occurrences / 26 files), which is meaningful because that count was not just re-read from a diff
but freshly re-derived with an independent grep methodology.

Two minor asymmetries worth noting for context, neither a defect:

- **M2-2 vs M2-3 guard style differs** (adapter-identity check vs. `appEnv`-only check) — both
  reports disclose this themselves and the reason (M2-2 has a real adapter to distinguish from the
  stub; M2-3 does not yet, since building a real PSP adapter is explicitly out of scope and shared
  with the still-open C2-2 Critical finding). Consistent with both reports' own text, not a hidden
  gap.
- **M2-6's storefront image is not yet referenced by `deploy.yml`'s pin/rollout step or signed by
  `release.yml`** — explicitly disclosed in `M2_6_REPORT.md`'s "Residual / Known Gaps" section as a
  pre-existing gap shared with the H-07 collector image, not introduced or worsened by this fix, and
  correctly out of M2-6's stated scope (deployability of the artifact, not full CD parity).

## Summary Verdict

M2-2 through M2-7 are genuinely closed: every commit exists, touches exactly the claimed files, and
the wiring/logic changes are real and functionally verified by direct source inspection (not stub
implementations, not cosmetic renames). M2-1's DEFERRED status is legitimate per the audit's own
rules — it is not silently fixed (zero Temporal importers, zero real `.signal()` call sites), and its
stated dependency on C2-2 (real PSP adapter) is corroborated by an independent source
(`POST_CRITICAL_VERIFICATION_REPORT.md`) as still genuinely open, not a convenient excuse invented
by this register.
