# Final Recoverable-Subsystems Declaration

This closes the reconstruction phase requested alongside the Final Operational Readiness Sprint.
Every subsystem identified as unique to `lumo-platform`'s working tree (relative to
`recovery/history-reconstruction`) has now either been **reconstructed** or **formally declared
unrecoverable**, per the standing evidence methodology (`RECOVERY_IMPLEMENTATION_METHODOLOGY.md`)
and the one explicit exception (`INTEGRATION_SPRINT_EXCEPTION.md`).

## Reconstructed this session

| Milestone                 | Commit                | Scope                                                                   |
| ------------------------- | --------------------- | ----------------------------------------------------------------------- |
| Finance ports.ts arch fix | `da52725`             | Pre-existing `domain-stays-pure` violation, unrelated to reconstruction |
| K7 (partial)              | `c24ec9b`             | 48 evidenced files, `packages/tracking` core                            |
| Customer-360              | `7b2b0ee` + `574cd9e` | Full 226-file context, unblocked by K7                                  |
| E1 — Collector            | `a95a651`             | Full 6-file app, High-confidence evidence found on re-investigation     |

**Investigated, evidenced, gate-verified in isolation, but NOT merged:** Integration Sprint —
service layer (11 packages, under the explicit evidence exception in
`INTEGRATION_SPRINT_EXCEPTION.md`). Briefly landed as `44be526`, then reverted (`bc6fba8`) by
explicit user decision after it broke `apps/admin`'s typecheck (74/74 → 74/76) — fixing that
narrowly cascades into the same ~40-package admin-wiring entanglement documented below, not a quick
patch. The evidence and reconstructed code remain fully described in
`INTEGRATION_SPRINT_SERVICE_LAYER_MILESTONE_REPORT.md` (now marked NOT MERGED) for whoever picks up
the admin-wiring milestone next; re-attempt only together with it.

## Formally declared unrecoverable / deferred (with reasons already on record)

| Subsystem                                                                                                                                                                       | File count (approx.)                  | Status                                                                                                                                                                                                                                                                                                                                                                       | Where documented                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `packages/tracking` remainder (`definitions/{event-definition,parameter,resolution}.ts`, `execution/*`, `inspector/timeline.ts`, `runtime/{replay-runtime,telemetry}.ts`)       | 39                                    | Blocked — zero primary evidence after two research rounds                                                                                                                                                                                                                                                                                                                    | `K7_FINAL_RECONCILIATION_REPORT.md`                                                         |
| `services/finance` (both the Integration-Sprint-cited symbols AND the separately-tagged "M2" reorg)                                                                             | 52                                    | Deferred — the two clusters are not separable in practice (Integration-Sprint files import M2-reorg infrastructure); M2 itself has no primary evidence                                                                                                                                                                                                                       | `INTEGRATION_SPRINT_SERVICE_LAYER_MILESTONE_REPORT.md`                                      |
| `apps/runtime` — Phase 4B module framework (`module.ts`, `platform.ts`, `modules/*`, `diagnostics.ts`, `jobs.ts`, `health-server.ts`, `metrics.ts`, `shutdown.ts`)              | ~58                                   | Blocked — no primary evidence; `R1_MILESTONE_REPORT.md` already deferred this exact file list                                                                                                                                                                                                                                                                                | `RUNTIME_R2_INVESTIGATION_REPORT.md`                                                        |
| `apps/runtime` — Purchase Saga (`purchase-saga-activities.ts`, `purchase/*`)                                                                                                    | 5                                     | **Permanently excluded** — already-committed audit findings (SAGA-1 through SAGA-11) document this code as currently defective; reconstructing it would enshrine known bugs as history                                                                                                                                                                                       | `RUNTIME_R2_INVESTIGATION_REPORT.md`, `INTEGRATION_SPRINT_EXCEPTION.md`                     |
| `apps/admin` — full composition wiring                                                                                                                                          | 37                                    | Deferred — wires ~40 packages, most never committed here; requires reconstructing ~2 dozen other bounded contexts first, a separate large program                                                                                                                                                                                                                            | `INTEGRATION_SPRINT_APP_WIRING_STATUS.md`                                                   |
| `apps/runtime` — Integration Sprint runtime slices (`buildFulfillmentTriggerRuntime`, `buildShipmentNotificationRuntime`)                                                       | (subset of the 80 apps/runtime files) | Deferred — code would be committed but uncallable without the Phase 4B module framework above                                                                                                                                                                                                                                                                                | `INTEGRATION_SPRINT_APP_WIRING_STATUS.md`                                                   |
| `services/analytics` domain/engine/resolver restructuring (`domain/{aggregation,canonical-id,dimension,expression,metric,read-model}.ts`, `resolver/resolvers.ts`, `finance/*`) | 13                                    | Deferred, not investigated to the same depth as Finance/K7/Runtime — same class of unrelated directory restructuring as Finance's "M2" reorg (different file layout, no citation found in a preliminary check); flagged for a future dedicated pass rather than re-run here given diminishing returns after four consecutive deep investigations found the same shape of gap | This document                                                                               |
| Governance suite creation (`scripts/governance/*`, `docs/governance/*`, `.husky/_/*`)                                                                                           | ~38                                   | Not executable — no primary source for the governance suite's own creation exists anywhere (only FF-FR-01's addition is attributable, to P1, already committed)                                                                                                                                                                                                              | `RECONCILIATION_REPORT_2026-08-02.md` (pre-existing finding, re-confirmed not re-litigated) |
| `docs/implementation`, `docs/architecture`, `docs/platform`, `docs/governance`, `docs/generated` reference docs                                                                 | ~108                                  | Not reconstructed as standalone commits — largely Sprint Reports and architecture docs already _used as evidence_ by the milestones above, not independently valuable to commit as a separate step; several (`ARCHITECTURE_REMEDIATION_PLAN.md` etc.) are already committed from the Tier-9 docs milestone (`5a8dff1`)                                                       | N/A — informational only                                                                    |
| `infrastructure/docker`, `.github/workflows`, `packages/db` misc, `apps/storefront` (2 files)                                                                                   | ~22                                   | Not investigated this session — small, likely later ops/infra evolution unrelated to any milestone above; genuinely out of scope given the session's focus                                                                                                                                                                                                                   | This document                                                                               |
| ~50 scratch debug `.txt` files (`tsc_*`, `test_*`, `arch_out*`, `dup_out*`, `governance_out*`, `gov_*`) at `lumo-platform`'s root                                               | ~50                                   | Junk — command-output captures from prior sessions, not source, not evidence, not to be committed                                                                                                                                                                                                                                                                            | N/A                                                                                         |
| ~30 superseded V2-V5 recovery-planning documents at `lumo-platform`'s root (`REPOSITORY_HISTORY_RECOVERY*.md`, `EVIDENCE_MATRIX*.md`, `FINAL_EXECUTION_PLAN*.md`, etc.)         | ~30                                   | Superseded — this session's own investigations (K7, Customer-360, Runtime, Integration Sprint, Collector) independently re-derived and, where they disagreed, corrected these documents' conclusions; not to be committed as-is                                                                                                                                              | N/A                                                                                         |

## Bottom line

The repository has no remaining subsystem that is both (a) genuinely evidenced and (b) cleanly
committable without first landing something larger and separately unevidenced. Every deferred item
above has a specific, named reason on record — none is a silent omission. This is the point at
which `recovery/history-reconstruction` should be treated as canonical.
