{
  "name": "@platform/analytics",
  "version": "0.0.0",
  "private": true,
  "description": "Analytics bounded context — the governed semantic layer (Core Engine, Sprint 3.2, D-064/D-065). Owns no business context; consumes read models by string name/field.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@clickhouse/client": "^1.7.0",
    "@platform/clickhouse": "workspace:*",
    "@platform/domain": "workspace:*",
    "@platform/types": "workspace:*",
    "@platform/utils": "workspace:*"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@types/node": "^22.7.5",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
