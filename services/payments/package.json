{
  "name": "@platform/payments",
  "version": "0.0.0",
  "private": true,
  "description": "Payments bounded context — payment intent, charge, refund; one PSP (Phase 1 Commerce Core).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@platform/application": "workspace:*",
    "@platform/contracts": "workspace:*",
    "@platform/db": "workspace:*",
    "@platform/domain": "workspace:*",
    "@platform/domain-events": "workspace:*",
    "@platform/messaging": "workspace:*",
    "@platform/repository": "workspace:*",
    "@platform/secrets": "workspace:*",
    "@platform/types": "workspace:*",
    "@platform/utils": "workspace:*",
    "@prisma/client": "^6.5.0"
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "@vitest/coverage-v8": "^2.1.4",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
